evap_fun <- approxfun(df$storage, df$evaporation)
evap_fun <- approxfun(df$storage, df$evaporation, rule = 2)
View(evaporation)
View(storage)
evap_data<- merge(evaporation, storage, by = "datetime")
sum(duplicated(evap_data$storage))
View(evap_data)
evap_data<- merge(evaporation[evaporation$evaporation > 0], storage, by = "datetime")
evap_data<- merge(evaporation[evaporation$evaporation > 0,], storage, by = "datetime")
evap_fun <- approxfun(df$storage, df$evaporation, rule = 2)
evap_data<- merge(evaporation[evaporation$evaporation > 0,], storage, by = "datetime")
evap_clean <- aggregate(evap ~ storage, data = evap_data, FUN = mean)
evap_data<- merge(evaporation[evaporation$evaporation > 0,], storage, by = "datetime")
evap_clean <- aggregate(evaporation ~ storage, data = evap_data, FUN = mean)
View(evap_clean)
evap_fun <- approxfun(df$storage, df$evaporation, rule = 2)
evap_fun <- approxfun(evap_clean$storage, evap_clean$evaporation, rule = 2)
evap_fun <- approxfun(evap_clean$storage, evap_clean$evaporation, rule = 2)
# Merge Data
additive <- merge(current_storage, inflow_proj, by = "datetime", all = TRUE)
subtractive <- merge(outflow_proj, evaporation, by= "datetime", all = TRUE)
df <- merge(additive, subtractive, by = "datetime", all = TRUE)
# Interpolate
time_grid <- seq(from = (current_storage$datetime-10), to = as.Date("2027-10-01"), by = "day")
inflow_most_i <- inflow_most_fun(time_grid)
evap_i <- evap_fun(time_grid)
outflow_most_i <- outflow_most_fun(time_grid)
df<- data.frame(datetime = time_grid, inflow = inflow_most_i, outflow = outflow_most_i, evap = evap_i)
df$storage_proj <- numeric(nrow(df))
## Iterate Projected Storage
start_idx <- which.min(abs(df$datetime - current_storage$datetime))
df$storage_proj[start_idx] <- current_storage$storage
for (i in (start_idx + 1):nrow(df)) {
evap_i <- evap_fun(df$storage_proj[i-1])
df$storage_proj[i] <-
df$storage_proj[i - 1] +
df$inflow[i] -
df$outflow[i] -
evap_i
}
plot(time_grid, df$storage_proj)
# Convert from storage to elevation
plot(time_grid, evap_i)
plot(time_grid, df$evaporation)
# Convert from storage to elevation
plot(time_grid, df$storage_proj)
plot(time_grid, evap_i)
plot(time_grid, evap_fun)
plot(time_grid, evap_fun(df$storage_proj))
plot(time_grid, df$evaporation
plot(time_grid, df$evaporation)
plot(time_grid, df$evaporation)
plot(df$datetime, df$evaporation)
plot(df$storage, df$evaporation)
plot(storage, evaporation)
# Interpolate
time_grid <- seq(from = (current_storage$datetime-10), to = as.Date("2027-10-01"), by = "month")
inflow_most_i <- inflow_most_fun(time_grid)
evap_i <- evap_fun(time_grid)
outflow_most_i <- outflow_most_fun(time_grid)
df<- data.frame(datetime = time_grid, inflow = inflow_most_i, outflow = outflow_most_i, evap = evap_i)
df$storage_proj <- numeric(nrow(df))
## Iterate Projected Storage
start_idx <- which.min(abs(df$datetime - current_storage$datetime))
df$storage_proj[start_idx] <- current_storage$storage
for (i in (start_idx + 1):nrow(df)) {
evap_i <- evap_fun(df$storage_proj[i-1])
df$storage_proj[i] <-
df$storage_proj[i - 1] +
df$inflow[i] -
df$outflow[i] -
evap_i
}
plot(time_grid, df$storage_proj)
evap_fun <- splinefun(evap_clean$storage, evap_clean$evaporation, method = "natural")
# Merge Data
additive <- merge(current_storage, inflow_proj, by = "datetime", all = TRUE)
subtractive <- merge(outflow_proj, evaporation, by= "datetime", all = TRUE)
df <- merge(additive, subtractive, by = "datetime", all = TRUE)
# Interpolate
time_grid <- seq(from = (current_storage$datetime-10), to = as.Date("2027-10-01"), by = "month")
inflow_most_i <- inflow_most_fun(time_grid)
evap_i <- evap_fun(time_grid)
outflow_most_i <- outflow_most_fun(time_grid)
df<- data.frame(datetime = time_grid, inflow = inflow_most_i, outflow = outflow_most_i, evap = evap_i)
df$storage_proj <- numeric(nrow(df))
## Iterate Projected Storage
start_idx <- which.min(abs(df$datetime - current_storage$datetime))
df$storage_proj[start_idx] <- current_storage$storage
for (i in (start_idx + 1):nrow(df)) {
evap_i <- evap_fun(df$storage_proj[i-1])
df$storage_proj[i] <-
df$storage_proj[i - 1] +
df$inflow[i] -
df$outflow[i] -
evap_i
}
plot(time_grid, df$storage_proj)
plot(time_grid, df$storage_proj)
elev_fun <- splinefun(df$storage, df$elevation, method = "natural")
elevation <- read.csv("Elevation.csv")
View(elevation)
elevation <- read.csv("Elevation.csv")
elevation$datetime <- as.Date(elevation$datetime)
elev_data<- merge(elevation, storage, by = "datetime")
elev_fun <- splinefun(elev_data$storage, elev_data$elevation, method = "natural")
inflow_most_i <- inflow_most_fun(time_grid)
outflow_most_i <- outflow_most_fun(time_grid)
for (i in (start_idx + 1):nrow(df)) {
df$elev_i <- elev_fun(df$storage_proj[i])
for (i in (start_idx):nrow(df)) {
df$elev_i <- elev_fun(df$storage_proj[i])
for (i in (start_idx):nrow(df)) {
df$elev <- elev_fun(df$storage_proj[i])
plot(time_grid, df$elev)
for (i in (start_idx):nrow(df)) {
df$elev_proj <- elev_fun(df$storage_proj[i])
plot(time_grid, df$elev)
plot(time_grid, df$elev_proj)
plot(time_grid, df$elev_proj)
df$days_in_step <- c(diff(df$datetime), NA)
## Iterate Projected Storage
df$days_in_step <- as.numeric(c(diff(df$datetime), NA))
for (i in (start_idx + 1):nrow(df)) {
evap_rate <-   evap_fun(df$storage_proj[i-1])
evap_total <- evap_rate *df$days_in_step
for (i in (start_idx + 1):nrow(df)) {
evap_rate <-   evap_fun(df$storage_proj[i-1])
evap_total <- evap_rate *df$days_in_step
df$storage_proj[i] <-
df$storage_proj[i - 1] +
df$inflow[i] -
df$outflow[i] -
evap_i
}
for (i in (start_idx):nrow(df)) {
df$elev_proj <- elev_fun(df$storage_proj[i])
plot(time_grid, df$elev_proj)
plot(time_grid, df$storage_proj)
View(df)
View(df)
df<- data.frame(datetime = time_grid, inflow = inflow_most_i, outflow = outflow_most_i, evap = evap_i)
df$storage_proj <- numeric(nrow(df))
df$days_in_step <- as.numeric(c(diff(df$datetime), NA))
start_idx <- which.min(abs(df$datetime - current_storage$datetime))
df$storage_proj[start_idx] <- current_storage$storage
for (i in (start_idx + 1):nrow(df)) {
evap_rate <-      evap_fun(df$storage_proj[i-1])
evap_total <- evap_rate *df$days_in_step
df$storage_proj[i] <-
df$storage_proj[i - 1] +
df$inflow[i] -
df$outflow[i] -
evap_i
}
for (i in (start_idx):nrow(df)) {
df$elev_proj <- elev_fun(df$storage_proj[i])
}
plot(time_grid, df$storage_proj)
start_idx <- which.min(abs(df$datetime - current_storage$datetime))
df$storage_proj[start_idx] <- current_storage$storage
df$storage_proj[i] <-
df$storage_proj[i - 1] +
df$inflow[i] -
df$outflow[i] -
evap_i
}
for (i in (start_idx):nrow(df)) {
df$elev_proj <- elev_fun(df$storage_proj[i])
plot(time_grid, df$storage_proj)
for (i in (start_idx + 1):nrow(df)) {
evap_rate_i <-        evap_fun(df$storage_proj[i-1])
evap_total_i <- evap_rate_i *df$days_in_step[i-1]
df$storage_proj[i] <-
df$storage_proj[i - 1] +
df$inflow[i] -
df$outflow[i] -
evap_total_i
}
for (i in (start_idx):nrow(df)) {
df$elev_proj <- elev_fun(df$storage_proj[i])
}
plot(time_grid, df$storage_proj)
plot(time_grid, df$storage_proj)
View(df)
plot(time_grid, df$storage_proj)
## Set Current Storage: Need to import current version of Storage.csv from https://usbr.gov/uc/water/hydrodata/reservoir_data/919/dashboard.html#csv/
storage <- read.csv("Storage.csv")
storage$datetime <- as.Date(storage$datetime)
current_storage <- storage[which.max(storage$datetime),]
# Set Projected Inflows
inflow <- read.csv("Inflow.csv")
inflow_proj <- data.frame(inflow$ESP.Min, inflow$ESP.Most, inflow$ESP.Max, as.Date(inflow$datetime, format = "%m/%d/%Y"))
names(inflow_proj)[4] <- "datetime"
inflow_most_fun <- approxfun(as.numeric(inflow_proj$datetime), inflow_proj$inflow.ESP.Most)
# Set Releases
outflow <- read.csv("Outflow.csv")
outflow_proj <- data.frame(outflow$ESP.Min, outflow$ESP.Most, as.Date(outflow$datetime, format = "%m/%d/%Y"))
names(outflow_proj)[3] <- "datetime"
outflow_most_fun <- approxfun(outflow_proj$datetime, outflow_proj$outflow.ESP.Most)
# Set Evaporation
evaporation <- read.csv("Evaporation.csv")
evaporation$datetime <-as.Date(evaporation$datetime)
evap_data<- merge(evaporation[evaporation$evaporation > 0,], storage, by = "datetime")
evap_clean <- aggregate(evaporation ~ storage, data = evap_data, FUN = mean)
evap_fun <- approxfun(evap_clean$storage, evap_clean$evaporation, rule = 2)
# Conversion from storage to elevation setup
elevation <- read.csv("Elevation.csv")
elevation$datetime <- as.Date(elevation$datetime)
elev_data<- merge(elevation, storage, by = "datetime")
elev_fun <- splinefun(elev_data$storage, elev_data$elevation, method = "natural")
# Merge Data
additive <- merge(current_storage, inflow_proj, by = "datetime", all = TRUE)
subtractive <- merge(outflow_proj, evaporation, by= "datetime", all = TRUE)
df <- merge(additive, subtractive, by = "datetime", all = TRUE)
# Interpolate
time_grid <- seq(from = (current_storage$datetime-10), to = as.Date("2027-10-01"), by = "month")
inflow_most_i <- inflow_most_fun(time_grid)
outflow_most_i <- outflow_most_fun(time_grid)
df<- data.frame(datetime = time_grid, inflow = inflow_most_i, outflow = outflow_most_i, evap = evap_i)
df$storage_proj <- numeric(nrow(df))
df$days_in_step <- as.numeric(c(diff(df$datetime), NA))
## Iterate Projected Storage
start_idx <- which.min(abs(df$datetime - current_storage$datetime))
df$storage_proj[start_idx] <- current_storage$storage
for (i in (start_idx + 1):nrow(df)) {
evap_rate_i <-        evap_fun(df$storage_proj[i-1])
evap_total_i <- evap_rate_i *df$days_in_step[i-1]
df$storage_proj[i] <-
df$storage_proj[i - 1] +
df$inflow[i] -
df$outflow[i] -
evap_total_i
}
for (i in (start_idx):nrow(df)) {
df$elev_proj <- elev_fun(df$storage_proj[i])
}
plot(time_grid, df$storage_proj)
View(df)
View(evaporation)
View(df)
df<- data.frame(datetime = time_grid, inflow = inflow_most_i, outflow = outflow_most_i)
df$storage_proj <- numeric(nrow(df))
df$days_in_step <- as.numeric(c(diff(df$datetime), NA))
df$days_in_step <- as.numeric(c(diff(df$datetime), NA))
class(df$datetime)
df$days_in_step <- as.numeric(c(diff(df$datetime), NA))
head(df$days_in_step)
df$days_in_step <- as.numeric(c(diff(df$datetime), NA))
head(df$datetime)
df$days_in_step <- as.numeric(c(diff(df$datetime), NA))
str(df$datetime)
df$days_in_step <- as.numeric(c(diff(df$datetime), NA))
diff(df$datetime)
df$days_in_step <- as.numeric(c(diff(df$datetime), NA))
diff(df$datetime)
df$days_in_step <- c(as.numeric(diff(df$datetime), NA))
diff(df$datetime)
df$days_in_step <- c(as.numeric(diff(df$datetime, NA)))
diff(df$datetime)
df$days_in_step <- c(as.numeric(diff(df$datetime)), NA)
## Iterate Projected Storage
start_idx <- which.min(abs(df$datetime - current_storage$datetime))
df$storage_proj[start_idx] <- current_storage$storage
for (i in (start_idx + 1):nrow(df)) {
evap_rate_i <-        evap_fun(df$storage_proj[i-1])
evap_total_i <- evap_rate_i *df$days_in_step[i-1]
df$storage_proj[i] <-
df$storage_proj[i - 1] +
df$inflow[i] -
df$outflow[i] -
evap_total_i
}
for (i in (start_idx):nrow(df)) {
df$elev_proj <- elev_fun(df$storage_proj[i])
}
plot(time_grid, df$storage_proj)
start_idx <- which.min(abs(df$datetime - current_storage$datetime))
df$storage_proj[start_idx] <- current_storage$storage
for (i in (start_idx + 1):nrow(df)) {
evap_rate_i <-        evap_fun(df$storage_proj[i-1])
evap_total_i <- evap_rate_i *df$days_in_step[i-1]
df$storage_proj[i] <-
df$storage_proj[i - 1] +
df$inflow[i] -
df$outflow[i] -
evap_total_i
}
plot(time_grid, df$storage_proj)
for (i in (start_idx + 1):nrow(df)) {
evap_rate_i <- evap_fun(df$storage_proj[i-1])
evap_total_i <- evap_rate_i *df$days_in_step[i-1]
df$storage_proj[i] <-
df$storage_proj[i - 1] +
df$inflow[i] -
df$outflow[i] -
evap_total_i
}
for (i in (start_idx):nrow(df)) {
df$elev_proj <- elev_fun(df$storage_proj[i])
plot(time_grid, df$storage_proj)
for (i in (start_idx):nrow(df)) {
df$elev_proj <- elev_fun(df$storage_proj[i])
}
elev_fun(current_storage$storage)
}
elev_fun(current_storage$storage)
elev_fun <- approxfun(elev_data$storage, elev_data$elevation, rule = 2)
elev_fun(current_storage$storage)
View(elevation)
elev_fun <- splinefun(elev_data$storage, elev_data$elevation, method = "monoH.FC")
elev_fun(current_storage$storage)
elev_fun <- splinefun(elev_data$storage, elev_data$elevation, method = "natural")
elev_fun(current_storage$storage)
View(elev_data)
elev_data<- merge(elevation, storage, by = "datetime")
elev_fun <- splinefun(elev_data$storage, elev_data$elevation, method = "natural")
elev_fun(current_storage$storage)
elev_fun <- splinefun(elev_data$storage, elev_data$pool.elevation, method = "natural")
elev_data<- merge(elevation, storage, by = "datetime")
elev_clean <- aggregate(pool.elevation ~ storage, data = elev_data, FUN = mean)
elev_fun <- splinefun(elev_data$storage, elev_data$pool.elevation, method = "natural")
elev_clean <- splinefun(elev_data$storage, elev_data$pool.elevation, method = "natural")
View(elev_data)
elev_fun <- splinefun(elev_clean$storage, elev_clean$pool.elevation, method = "natural")
elev_data<- merge(elevation, storage, by = "datetime")
elev_clean <- aggregate(pool.elevation ~ storage, data = elev_data, FUN = mean)
elev_fun <- splinefun(elev_clean$storage, elev_clean$pool.elevation, method = "natural")
elev_fun(current_storage$storage)
View(elevation)
for (i in (start_idx):nrow(df)) {
df$elev_proj <- elev_fun(df$storage_proj[i])
elev_fun <- splinefun(elev_clean$storage, elev_clean$pool.elevation, method = "natural")
for (i in (start_idx):nrow(df)) {
df$elev_proj <- elev_fun(df$storage_proj[i])
elev_fun <- approxfun(elev_clean$storage, elev_clean$pool.elevation, rule = 2)
for (i in (start_idx + 1):nrow(df)) {
evap_rate_i <- evap_fun(df$storage_proj[i-1])
evap_total_i <- evap_rate_i *df$days_in_step[i-1]
df$storage_proj[i] <-
df$storage_proj[i - 1] +
df$inflow[i] -
df$outflow[i] -
evap_total_i
}
for (i in (start_idx):nrow(df)) {
df$elev_proj <- elev_fun(df$storage_proj[i])
## Set Current Storage: Need to import current version of Storage.csv from https://usbr.gov/uc/water/hydrodata/reservoir_data/919/dashboard.html#csv/
storage <- read.csv("Storage.csv")
storage$datetime <- as.Date(storage$datetime)
current_storage <- storage[which.max(storage$datetime),]
# Set Projected Inflows
inflow <- read.csv("Inflow.csv")
inflow_proj <- data.frame(inflow$ESP.Min, inflow$ESP.Most, inflow$ESP.Max, as.Date(inflow$datetime, format = "%m/%d/%Y"))
names(inflow_proj)[4] <- "datetime"
inflow_most_fun <- approxfun(as.numeric(inflow_proj$datetime), inflow_proj$inflow.ESP.Most)
# Set Releases
outflow <- read.csv("Outflow.csv")
outflow_proj <- data.frame(outflow$ESP.Min, outflow$ESP.Most, as.Date(outflow$datetime, format = "%m/%d/%Y"))
names(outflow_proj)[3] <- "datetime"
outflow_most_fun <- approxfun(outflow_proj$datetime, outflow_proj$outflow.ESP.Most)
# Set Evaporation
evaporation <- read.csv("Evaporation.csv")
evaporation$datetime <-as.Date(evaporation$datetime)
evap_data<- merge(evaporation[evaporation$evaporation > 0,], storage, by = "datetime")
evap_clean <- aggregate(evaporation ~ storage, data = evap_data, FUN = mean)
evap_fun <- approxfun(evap_clean$storage, evap_clean$evaporation, rule = 2)
# Conversion from storage to elevation setup
elevation <- read.csv("Elevation.csv")
elevation$datetime <- as.Date(elevation$datetime)
elev_data<- merge(elevation, storage, by = "datetime")
elev_clean <- aggregate(pool.elevation ~ storage, data = elev_data, FUN = mean)
elev_fun <- approxfun(elev_clean$storage, elev_clean$pool.elevation, rule = 2)
# Merge Data
additive <- merge(current_storage, inflow_proj, by = "datetime", all = TRUE)
subtractive <- merge(outflow_proj, evaporation, by= "datetime", all = TRUE)
df <- merge(additive, subtractive, by = "datetime", all = TRUE)
# Interpolate
time_grid <- seq(from = (current_storage$datetime-10), to = as.Date("2027-10-01"), by = "month")
inflow_most_i <- inflow_most_fun(time_grid)
outflow_most_i <- outflow_most_fun(time_grid)
df<- data.frame(datetime = time_grid, inflow = inflow_most_i, outflow = outflow_most_i)
df$storage_proj <- numeric(nrow(df))
diff(df$datetime)
df$days_in_step <- c(as.numeric(diff(df$datetime)), NA)
## Iterate Projected Storage
start_idx <- which.min(abs(df$datetime - current_storage$datetime))
df$storage_proj[start_idx] <- current_storage$storage
for (i in (start_idx + 1):nrow(df)) {
evap_rate_i <- evap_fun(df$storage_proj[i-1])
evap_total_i <- evap_rate_i *df$days_in_step[i-1]
df$storage_proj[i] <-
df$storage_proj[i - 1] +
df$inflow[i] -
df$outflow[i] -
evap_total_i
}
for (i in (start_idx):nrow(df)) {
df$elev_proj <- elev_fun(df$storage_proj[i])
}
plot(time_grid, df$storage_proj)
for (i in (start_idx + 1):nrow(df)) {
evap_rate_i <- evap_fun(df$storage_proj[i-1])
evap_total_i <- evap_rate_i *df$days_in_step[i-1]
for (i in (start_idx + 1):nrow(df)) {
evap_rate_i <- evap_fun(df$storage_proj[i-1])
evap_total_i <- evap_rate_i *df$days_in_step[i-1]
View(df)
View(df)
## Set Current Storage: Need to import current version of Storage.csv from https://usbr.gov/uc/water/hydrodata/reservoir_data/919/dashboard.html#csv/
storage <- read.csv("Storage.csv")
storage$datetime <- as.Date(storage$datetime)
current_storage <- storage[which.max(storage$datetime),]
# Set Projected Inflows
inflow <- read.csv("Inflow.csv")
inflow_proj <- data.frame(inflow$ESP.Min, inflow$ESP.Most, inflow$ESP.Max, as.Date(inflow$datetime, format = "%m/%d/%Y"))
names(inflow_proj)[4] <- "datetime"
inflow_most_fun <- approxfun(as.numeric(inflow_proj$datetime), inflow_proj$inflow.ESP.Most)
# Set Releases
outflow <- read.csv("Outflow.csv")
outflow_proj <- data.frame(outflow$ESP.Min, outflow$ESP.Most, as.Date(outflow$datetime, format = "%m/%d/%Y"))
names(outflow_proj)[3] <- "datetime"
outflow_most_fun <- approxfun(outflow_proj$datetime, outflow_proj$outflow.ESP.Most)
# Set Evaporation
evaporation <- read.csv("Evaporation.csv")
evaporation$datetime <-as.Date(evaporation$datetime)
evap_data<- merge(evaporation[evaporation$evaporation > 0,], storage, by = "datetime")
evap_clean <- aggregate(evaporation ~ storage, data = evap_data, FUN = mean)
evap_fun <- approxfun(evap_clean$storage, evap_clean$evaporation, rule = 2)
# Conversion from storage to elevation setup
elevation <- read.csv("Elevation.csv")
elevation$datetime <- as.Date(elevation$datetime)
elev_data<- merge(elevation, storage, by = "datetime")
elev_clean <- aggregate(pool.elevation ~ storage, data = elev_data, FUN = mean)
elev_fun <- approxfun(elev_clean$storage, elev_clean$pool.elevation, rule = 2)
# Merge Data
additive <- merge(current_storage, inflow_proj, by = "datetime", all = TRUE)
subtractive <- merge(outflow_proj, evaporation, by= "datetime", all = TRUE)
df <- merge(additive, subtractive, by = "datetime", all = TRUE)
# Interpolate
time_grid <- seq(from = (current_storage$datetime-10), to = as.Date("2027-10-01"), by = "month")
inflow_most_i <- inflow_most_fun(time_grid)
outflow_most_i <- outflow_most_fun(time_grid)
df<- data.frame(datetime = time_grid, inflow = inflow_most_i, outflow = outflow_most_i)
df$storage_proj <- numeric(nrow(df))
diff(df$datetime)
df$days_in_step <- c(as.numeric(diff(df$datetime)), NA)
## Iterate Projected Storage
start_idx <- which.min(abs(df$datetime - current_storage$datetime))
df$storage_proj[start_idx] <- current_storage$storage
for (i in (start_idx + 1):nrow(df)) {
evap_rate_i <- evap_fun(df$storage_proj[i-1])
evap_total_i <- evap_rate_i *df$days_in_step[i-1]
df$storage_proj[i] <-
df$storage_proj[i - 1] +
df$inflow[i] -
df$outflow[i] -
evap_total_i
df$elev_proj[i] <- elev_fun(df$storage_proj[i])
}
View(df)
View(current_storage)
View(df)
for (i in (start_idx + 1):nrow(df)) {
evap_rate_i <- evap_fun(df$storage_proj[i-1])
evap_total_i <- evap_rate_i *df$days_in_step[i-1]
df$storage_proj[i] <-
df$storage_proj[i - 1] +
df$inflow[i] -
df$outflow[i] -
evap_total_i
df$elev_proj[i] <- elev_fun(df$storage_proj[i])
df<- data.frame(datetime = time_grid, inflow = inflow_most_i, outflow = outflow_most_i)
df$storage_proj <- rep(NA, nrow(df))
diff(df$datetime)
df$days_in_step <- c(as.numeric(diff(df$datetime)), NA)
## Iterate Projected Storage
start_idx <- which.min(abs(df$datetime - current_storage$datetime))
df$storage_proj[start_idx] <- current_storage$storage
for (i in (start_idx + 1):nrow(df)) {
evap_rate_i <- evap_fun(df$storage_proj[i-1])
evap_total_i <- evap_rate_i *df$days_in_step[i-1]
df$storage_proj[i] <-
df$storage_proj[i - 1] +
df$inflow[i] -
df$outflow[i] -
evap_total_i
df$elev_proj[i] <- elev_fun(df$storage_proj[i])
}
plot(time_grid, df$storage_proj)
df<- data.frame(datetime = time_grid, inflow = inflow_most_i, outflow = outflow_most_i)
df$storage_proj <- rep(NA, nrow(df))
diff(df$datetime)
df$days_in_step <- c(as.numeric(diff(df$datetime)), NA)
## Iterate Projected Storage
start_idx <- which.min(abs(df$datetime - current_storage$datetime))
df$storage_proj[start_idx] <- current_storage$storage
for (i in (start_idx + 1):nrow(df)) {
evap_rate_i <- evap_fun(df$storage_proj[i-1])
evap_total_i <- evap_rate_i *df$days_in_step[i-1]
df$storage_proj[i] <-
df$storage_proj[i - 1] +
df$inflow[i] -
df$outflow[i] -
evap_total_i
df$elev_proj[i] <- elev_fun(df$storage_proj[i])
}
View(df)
View(df)
df$elev_proj <-elev_fun(df$storage_proj)
View(df)
View(df)
